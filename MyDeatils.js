const MyDetails= (props)=>{
    return <div>
        <h3>-*-*-*--*-*-*-*-*-*--*-*-*-*-*-*-*-*-*-*-*-*-*--*--*-*-*-</h3>

    <h3>CDAC YCP PUNE</h3>
    <p>My Name is :{props.myName} </p>
    <p>contact me : {props.email}</p>
    <p>  address is :{props.address} </p>
   <p>  my role is :{props.Role}</p>

    </div>
}

export default MyDetails;